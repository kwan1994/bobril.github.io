import * as b from 'bobril';
import * as color from '../color/lib';
import * as fontStyle from '../fontStyle/lib';

export const sidebarWrapper = b.styleDef({
    width: '100%',
    height: '100%',
    background: color.color03
});

export const sidebarWrapperContent = b.styleDef({
    paddingTop: 60,
    paddingBottom: 75,
    background: color.color04
});

export const sidebarLogo = b.styleDef({
    margin: 'auto',
    width: 130,
    height: 116
});

export const sidebarItems = b.styleDef({

});

export const sidebarItem = b.styleDef({

});